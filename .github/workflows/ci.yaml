name: Cedra CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        sanitizer: [NONE, ASAN, TSAN, UBSAN]
        include:
          # Bind compiler to OS
          - os: ubuntu-latest
            compiler: { cc: gcc, cxx: g++ }
          - os: ubuntu-latest
            compiler: { cc: clang, cxx: clang++ }
          - os: windows-latest
            compiler: { cc: cl, cxx: cl }
        exclude:
          # TSAN doesn't really supported by MSVC
          - os: windows-latest
            sanitizer: TSAN
          - os: windows-latest
            sanitizer: UBSAN
          # TSAN with GCC is often unstable in CI environments due to
          # libtsan version mismatches and high memory consumption.
          # Clang's TSAN is more mature and sufficient for data race detection.
          - compiler: { cc: gcc, cxx: g++ }
            sanitizer: TSAN

    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build clang g++

      - name: Install Dependencies (Windows)
        if: runner.os == 'Windows'
        run: |
          # Ninja обычно уже есть, но choco гарантирует наличие
          choco install ninja

      - name: Configure CMake
        # Using bash so ${{ matrix.sanitizer == 'ASAN' && 'ON' || 'OFF' }} should work
        shell: bash
        env:
          CC: ${{ matrix.compiler.cc }}
          CXX: ${{ matrix.compiler.cxx }}
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Debug \
            -DCDR_WITH_ASAN=${{ matrix.sanitizer == 'ASAN' && 'ON' || 'OFF' }} \
            -DCDR_WITH_TSAN=${{ matrix.sanitizer == 'TSAN' && 'ON' || 'OFF' }} \
            -DCDR_WITH_UBSAN=${{ matrix.sanitizer == 'UBSAN' && 'ON' || 'OFF' }} \
            -DCDR_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build -j 2

      - name: Run Tests
        working-directory: build
        run: ctest --output-on-failure -j 2